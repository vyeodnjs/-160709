<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hanbit.user.myapp160709">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ImageActivity" />
        <activity android:name=".calc.CalendarActivity" />
        <activity android:name=".calc.CalcActivity" />
        <activity android:name=".kaup.KaupActivity" />
        <activity android:name=".member.JoinActivity" />
        <activity android:name=".member.LoginActivity" />
        <activity android:name=".contact.ContactActivity"></activity>
    </application>

</manifest>